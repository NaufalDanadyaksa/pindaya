# Groq API Key (Required for AI Scan & Chat)
# Get your free key at: https://console.groq.com/keys
GROQ_API_KEY=

# Google Gemini API Key (Optional / Deprecated)
# Used only if fallback logic is enabled, but Groq is now primary.
GEMINI_API_KEY=
